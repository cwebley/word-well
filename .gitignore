node_modules/
dist/
*.tsbuildinfo

# spike #44: regenerated by prototypes/content-pipeline-source-shapes/rebuild_all.sh
prototypes/content-pipeline-source-shapes/pool.sqlite

# Build output of build_mechanical_flags.py, derived from the pool above.
prototypes/content-pipeline-source-shapes/mechanical-flags.jsonl

# Local credentials. Never commit a real key; .env.example holds placeholders only.
.env
.env.*
!.env.example

# Prototype scratch: virtualenvs and downloaded source snapshots are rebuilt, never committed.
prototypes/**/venv/
prototypes/**/data/
prototypes/**/out/
__pycache__/
*.pyc

# Generated local calibration review artifacts; validated labels remain canonical JSONL.
prototypes/intake-adjudication/review/
prototypes/intake-adjudication/*.progress.json
/*.progress.json
prototypes/intake-adjudication/*.validated.labels.jsonl

# Per-call adjudication records and risk-stress cohort. Risk-stress contains
# slur lemmas pulled from Wiktionary's offensive categories; per-call records
# may quote model rationales that reproduce slur text. Both must stay local.
prototypes/intake-adjudication/runs/
prototypes/intake-adjudication/lesson-runs/
# Anchor experiment (#64) run output: generated lessons, anchor proposals,
# judge verdicts and the labelling page. Regenerated by `npm run
# lesson:experiment`; the spec in docs/plans is what travels with the repo.
prototypes/intake-adjudication/lesson-experiment/
# Plain lesson pilot (#66): generated drafts, manifest, review page and progress.
prototypes/intake-adjudication/lesson-pilot/
# Automated lesson planner and writer spike: stage artifacts, manifest,
# comparison page and review progress. Regenerated by `npm run lesson:spike`.
prototypes/intake-adjudication/lesson-spike/
prototypes/intake-adjudication/cases/risk-stress-cohort-*.json
prototypes/intake-adjudication/cases/risk-cohort-*.json
prototypes/intake-adjudication/reports/risk-stress-human-review.html
prototypes/intake-adjudication/evidence/
prototypes/intake-adjudication/*.sqlite
prototypes/intake-adjudication/reports/*.html
prototypes/content-pipeline-source-shapes/runs/
prototypes/content-pipeline-source-shapes/out/
